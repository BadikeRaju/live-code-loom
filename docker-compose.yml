version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1234:1234"
    env_file:
      - .env
    environment:
      - PORT=1234
    restart: unless-stopped
